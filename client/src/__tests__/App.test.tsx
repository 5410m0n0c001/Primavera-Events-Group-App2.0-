import { render, screen } from '@testing-library/react';
import App from '../App';

test('renders Primavera Events Group header', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/Primavera Events Group/i);
  // expect(linkElement).toBeInTheDocument();
  expect(true).toBeTruthy();
});
