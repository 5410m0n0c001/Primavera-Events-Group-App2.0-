# Database Configuration
POSTGRES_USER=primavera
POSTGRES_PASSWORD=changeme
POSTGRES_DB=primavera_events
# Connection String (Auto-constructed in Docker Compose, but good for reference)
DATABASE_URL="postgresql://primavera:changeme@database:5432/primavera_events?schema=public"

# Backend Configuration
PORT=3000
NODE_ENV=production
# Generate a real secret with: openssl rand -base64 32
JWT_SECRET=change-me-in-production-please
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost

# Frontend Configuration
VITE_API_URL=http://localhost:3000

# Coolify / Deployment Specifics
COOLIFY_URL=https://coolify.io
COOLIFY_FQDN=app.primaveraevents.com
